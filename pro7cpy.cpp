#include<iostream>
#include<math.h>
using namespace std;

int main()
{
  int n, i = 0, p, max;
  cin>>n;
  
//   while(pow(2, i) < n)
//   {
//     i++;
//   }
  
  
  
  p = (log10(max))/(log10(2));
  
  cout<<(n - pow(2, p));
  
  return 0;
}
